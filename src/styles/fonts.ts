import { Andika, Fira_Code } from "next/font/google";

export const firaCode = Fira_Code({ variable: "--firaCode", subsets: ["latin"] });
export const andika = Andika({ variable: "--andika", subsets: ["latin"], weight: ["400", "700"] });
