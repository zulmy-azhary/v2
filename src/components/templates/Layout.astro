---
import Navbar from '@components/organisms/Navbar.astro';

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Zulmy Azhary AS | {title}</title>
  </head>
  <body class="relative font-firaCode bg-bgColor min-h-screen text-white max-w-xs md:max-w-2xl lg:max-w-4xl xl:max-w-6xl mx-auto flex flex-col gap-y-20 overflow-x-hidden">
    <Navbar />
    <slot />
  </body>
</html>
