---
interface Props {
  class?: string;
  src: string;
  alt: string;
}

const { src, alt, class: className } = Astro.props;
---

<img class:list={["select-none object-contain", className]} src={src} alt={alt} />
